spring:
  datasource:
    url: ${DB_URL}
    username:
    password:
  jpa:
    properties:
      hibernate:
        default_schema: public
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
      show-sql: true

app:
  version: 1.0.0
